import chalk from "chalk";

export async function storePage({ inquirer }) {
    console.log(chalk.red("Could not initialize authenticator module. Access to https://menuify.microart.cf/store/featured is not denied."));
}